# Run Ledger

One line per run. Keep config + artifacts in the same order.

Template:
YYYY-MM-DD HH:MM | branch= | commit= | mode=batched|header-only | lock_ms= | batch_blocks= | batch_ops= | batch_bytes= | batch_ms= | batch_bodies= | simpa_db= | simpa_args= | log= | csv= | notes=
